matrix:
  include:
    - language: java
      script:
      - mvn package 

  include:
    - language: node_js
      node_js: '9'
      before_script:
      - npm install -g tsc
      - npm install -g vsce
      script:
      - cd vsc-extension
      - vsce package -o build.vsix

